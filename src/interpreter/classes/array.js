import { TLabel } from './label.js'

export class TArray extends TLabel {

  constructor () {
    super(...arguments)
    this.size = 0
    this.length = 0
  }

  clear () {
  }

  get (idx) {
  }

  set (idx, value) {
  }

  push (value) {
  }

  pop () {
  }

}
